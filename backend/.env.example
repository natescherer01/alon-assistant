# ========================================
# Personal AI Assistant - Environment Configuration
# ========================================
# SaaS Model: Company provides Anthropic API key, users pay for service access

# ====================
# REQUIRED - Security
# ====================

# SECRET_KEY: Used for JWT token signing (REQUIRED)
# Generate with: python -c 'import secrets; print(secrets.token_urlsafe(32))'
SECRET_KEY=your-secret-key-here-minimum-32-characters

# ====================
# REQUIRED - Anthropic API
# ====================

# ANTHROPIC_API_KEY: System-wide Claude API key (company pays for this)
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ====================
# OPTIONAL - Database
# ====================

# SQLite (development default):
DATABASE_URL=sqlite:///./personal_assistant.db

# PostgreSQL (production - Railway auto-configures this):
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# ====================
# OPTIONAL - Redis
# ====================

# Redis for token blacklist and caching (optional but recommended)
# Railway auto-configures this when you add Redis addon
REDIS_URL=redis://localhost:6379/0

# ====================
# OPTIONAL - CORS Origins
# ====================

# Comma-separated list of allowed frontend origins (NO SPACES)
# Development:
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8000

# Production example:
# CORS_ORIGINS=https://your-app.vercel.app,https://your-domain.com

# ====================
# OPTIONAL - Token Expiration
# ====================

# Access token expiration (short-lived for security)
ACCESS_TOKEN_EXPIRE_MINUTES=60  # 1 hour (production default)

# Refresh token expiration (long-lived)
REFRESH_TOKEN_EXPIRE_DAYS=30  # 30 days

# ====================
# OPTIONAL - Environment
# ====================

ENVIRONMENT=development  # development | production
LOG_LEVEL=INFO  # DEBUG | INFO | WARNING | ERROR | CRITICAL

# ========================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ========================================
# Before deploying to Railway:
#
# 1. [ ] Set SECRET_KEY (generate secure 32+ char key)
# 2. [ ] Set ANTHROPIC_API_KEY (your company API key)
# 3. [ ] Set ENVIRONMENT=production
# 4. [ ] Use PostgreSQL DATABASE_URL (Railway provides this)
# 5. [ ] Use Redis REDIS_URL (Railway provides this)
# 6. [ ] Set CORS_ORIGINS to your actual frontend domain(s)
# 7. [ ] Verify ACCESS_TOKEN_EXPIRE_MINUTES=60 (not higher)
# 8. [ ] Ensure HTTPS is enabled (Railway does automatically)
# 9. [ ] Remove .env file from production (use Railway env vars)
#
# See DEPLOYMENT.md for complete guide
